generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  // Use DATABASE_URL for pooled connections (port 6543) in your app
  url       = env("DATABASE_URL")
  // Use DIRECT_URL for migrations and admin tasks (port 5432)
  directUrl = env("DIRECT_URL")
}
