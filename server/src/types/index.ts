/**
 * Types re-exported from frontend
 * 
 * NOTE: In a future monorepo setup, these types should be moved to a shared package
 * that both the frontend and backend can depend on.
 */

// Re-export frontend types - paths will need to be adjusted when moving to monorepo
export * from '../../../src/types/prismaTypes';
export * from '../../../src/types/dtoTypes';
