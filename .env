# Base Environment Variables (common to all environments)
PORT=5174
VITE_API_BASE_URL=http://localhost:3001

# Default database selection (can be overridden in environment-specific files or .env.local)
VITE_DATABASE_SELECTION=DEV

